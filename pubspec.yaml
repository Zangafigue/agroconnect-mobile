name: agroconnect_mobile
description: "Application mobile AgroConnect BF — Plateforme agricole B2B/B2C Burkina Faso"
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.1
  provider: ^6.1.2
  flutter_secure_storage: ^9.0.0
  google_nav_bar: ^5.0.6
  google_maps_flutter: ^2.6.0
  geolocator: ^11.0.0
  intl: ^0.19.0
  url_launcher: ^6.2.5
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1
  flutter_spinkit: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
